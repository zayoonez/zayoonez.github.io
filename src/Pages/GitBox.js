import React, { Component } from "react";
import styled from "styled-components";

const GitBoxContainer = styled.div`
  background-color: black;
  width: 100%;
  height: 200px;
`;
const GitBox = () => {
  <GitBoxContainer>{/* < */}</GitBoxContainer>;
};

export default GitBox;
